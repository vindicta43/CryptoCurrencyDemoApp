<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CryptoCurrencyDemoApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="floatingActionButtonStyle">@style/FabShapeStyle</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>

    <style name="FabShapeStyle" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">100dp</item>
        <!--        <item name="android:layout_gravity">center</item>-->
        <!--        <item name="android:gravity">center</item>-->
    </style>

    <style name="CustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">13sp</item>
        <item name="fontFamily">@font/montserrat_semibold</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="CustomTabChartText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">11sp</item>
        <item name="fontFamily">@font/montserrat_medium</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="Theme.CryptoCurrencyDemoApp" parent="Base.Theme.CryptoCurrencyDemoApp" />
</resources>